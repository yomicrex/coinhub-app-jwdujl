// Admin routes have been removed from the system.
// Users can only reset their own password via the standard authentication flow.
// No admin endpoints or functionality are available.

export function registerAdminRoutes() {
  // This function is intentionally empty.
  // All admin functionality has been removed.
}
